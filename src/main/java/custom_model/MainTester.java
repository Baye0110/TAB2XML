package custom_model;

import javafx.application.Application;

// the purpose of this class is to separate the main launch method in MainGraphicTest.java

public class MainTester {
	 public static void main(String[] args) {
		 Application.launch(MainGraphicsTest.class);
	 }
}
